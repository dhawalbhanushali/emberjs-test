<div class="container">
  <div class="menu">
    {{#link-to 'index'}}
      <h1>
        <em>SuperRentals</em>
      </h1>
    {{/link-to}}
    <div class="links">
      {{#link-to 'about'}}
        About
      {{/link-to}}
      {{#link-to 'contact'}}
        Contact
      {{/link-to}}
      {{#if session.isAuthenticated}}
          {{#link-to 'rentals.add'}}
            Add Rental
          {{/link-to}}
      {{/if}}
    </div>
  </div>
  <div class="body">
    {{outlet}}
  </div>
</div>
